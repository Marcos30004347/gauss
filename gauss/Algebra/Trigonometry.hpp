#include "Expression.hpp"

namespace alg {
namespace trig {

expr sinh(expr x);

expr cosh(expr x);

expr tanh(expr x);

expr cos(expr x);

expr sin(expr x);

expr tan(expr x);

expr csc(expr x);

expr cot(expr x);

expr sec(expr x);

expr coth(expr x);

expr sech(expr x);

expr csch(expr x);

expr arccos(expr x);

expr arcsin(expr x);

expr arctan(expr x);

expr arccot(expr x);

expr arcsec(expr x);

expr arccsc(expr x);

expr arccosh(expr x);

expr arctanh(expr x);

}
}
