name: Gauss build and test

on: push

jobs:
  gausscpp:
    runs-on: ubuntu-latest
    environment: deploy
    steps:
    - name: install cmake
      run: sudo apt-get install cmake
    - name: build project
      run: make
    - name: run tests
      run: make run-tests

