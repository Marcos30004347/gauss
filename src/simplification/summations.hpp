#ifndef SIMPLIFICATION_SUMMATION_H
#define SIMPLIFICATION_SUMMATION_H

#include "algebra/expression.hpp"

namespace simplification {

algebra::expression* simplify_summation(const algebra::expression* u);

}

#endif